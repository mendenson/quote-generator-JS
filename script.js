// Get Quotes From API
async function getQuotes() {
    const apiURL = './quotes.json';
    try {
        const response = await fetch(apiURL);
    } catch (error) {
        // Catch Error Here
    }
}